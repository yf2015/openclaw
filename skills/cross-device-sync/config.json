{
  "name": "cross-device-sync",
  "description": "通过GitHub仓库实现OpenClaw跨设备记忆同步",
  "version": "1.0.0",
  "author": "CL Assistant",
  "functions": [
    {
      "name": "setup_cross_device_sync",
      "description": "初始化跨设备同步设置",
      "parameters": {
        "type": "object",
        "properties": {
          "repoUrl": {
            "type": "string",
            "description": "GitHub仓库URL"
          },
          "token": {
            "type": "string",
            "description": "GitHub Personal Access Token"
          }
        },
        "required": ["repoUrl", "token"]
      }
    },
    {
      "name": "sync_now",
      "description": "立即执行跨设备同步",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "check_sync_status",
      "description": "检查同步状态",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  ]
}